--- a/AndroidManifest.xml
+++ b/AndroidManifest.xml
@@ -4,8 +4,6 @@
         coreApp="true"
         android:sharedUserId="android.uid.system">
 
-  	<uses-library android:name="org.apache.http.legacy" android:required="false" />
-
     <original-package android:name="com.android.settings" />
 
     <protected-broadcast android:name="com.android.settings.CARRIER_PROVISIONING" />
@@ -114,6 +112,13 @@
             android:defaultToDeviceProtectedStorage="true"
             android:directBootAware="true">
 
+        <!-- CM/RR platform classes (CMPartsPreference etc). Must live under
+             <application> - PackageParser rejects <manifest>-level uses-library
+             ("Unknown element under <manifest>"), and without this Settings
+             crash-loops inflating CMPartsPreference in Display/Battery. -->
+        <uses-library android:name="org.cyanogenmod.platform" android:required="true" />
+        <uses-library android:name="org.apache.http.legacy" android:required="false" />
+
         <!-- Settings -->
 
         <activity android:name="Settings"
--- a/AndroidManifest.xml
+++ b/AndroidManifest.xml
@@ -50,6 +50,8 @@
             android:defaultToDeviceProtectedStorage="true"
             android:directBootAware="true">
 
+        <uses-library android:name="org.cyanogenmod.platform" android:required="true" />
+
         <activity android:name=".PartsActivity"
                 android:configChanges="orientation|keyboardHidden|screenSize">
             <intent-filter>
--- a/TouchscreenGestureSettings.java
+++ b/TouchscreenGestureSettings.java
@@ -139,6 +139,11 @@
 
         final CMHardwareManager manager = CMHardwareManager.getInstance(context);
         final TouchscreenGesture[] gestures = manager.getTouchscreenGestures();
+        // Some devices report gesture support via the hardware feature but
+        // return a null gesture list (BootReceiver NPE at boot on u5a).
+        if (gestures == null || gestures.length == 0) {
+            return;
+        }
         final int[] actionList = buildActionList(context, gestures);
         for (final TouchscreenGesture gesture : gestures) {
             manager.setTouchscreenGestureEnabled(gesture, actionList[gesture.id] > 0);
